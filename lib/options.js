/*
  @desc Default options to be passed to each PythonShell
*/

const defaultOptions = {
    mode: 'text',
    pythonOptions: ['-u'], // get print results in real-time
    scriptPath: './scripts/'
}

module.exports = defaultOptions